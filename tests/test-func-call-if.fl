fn char_at(s: string, i: i32) -> string {
  return ""
}

fn test() -> i32 {
  var pos: i32 = 5
  if char_at("hello", pos) == "\\" {
    pos = pos + 1
  } else {
    pos = pos + 2
  }
  return 0
}

var x: i32 = test()
